export type PostItem = {
    id: string
    title: string
    date: string
    category: string
    shortDescription: string
    data: string[]
}

export type ProjectItem = {
    id: string
    title: string
    date: string
    category: string
    shortDescription: string
    data: string[]
}